import React from 'react';

export default function AboutUs({ onOpenConsultation }) {
  return (
    <section id="about" className="scroll-mt-24 w-full">
      {/* Vacant About Section — Ready for new content */}
    </section>
  );
}
